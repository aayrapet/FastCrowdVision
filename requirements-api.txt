# ─────────────────────────────────────────────────────────────────
# Dépendances UNIQUEMENT pour l'API (inférence + serveur).
# Ne PAS inclure : wandb, kagglehub, s3fs, duckdb, pyarrow,
#   torchaudio, torchmetrics, faster-coco-eval (entraînement seul).
# ─────────────────────────────────────────────────────────────────

# Deep learning — inférence seulement (pas torchaudio)
torch
torchvision

# Vision & traitement image
pillow
numpy>=1.26,<2.2
opencv-python-headless

# Modèle & poids
huggingface_hub
pyyaml

# API
fastapi
uvicorn[standard]
python-multipart

# Tracking
norfair
